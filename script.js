//your JS code here. If required.

const student = {
	name : "John",
	age : 21,
	city : "Noida"
}

function getKeys(obj) {
	return Object.keys(obj);
}

let keys = getKeys(student);
console.log(student);